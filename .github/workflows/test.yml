name: test

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

on:
  push:
    branches:
      - 'master'
  pull_request:
    branches:
      - 'master'

env:
  BUILD_TAG: nextcloud:test
  CONTAINER_NAME: nextcloud

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        docker_file:
          - v31.Dockerfile
          - v32.Dockerfile
          - v33.Dockerfile
        dir:
          - mysql
          - pgsql
          - sqlite
    steps:
      -
        name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      -
        name: Build
        uses: docker/bake-action@a66e1c87e2eca0503c343edf1d208c716d54b8a8 # v7.1.0
        with:
          source: .
          targets: image-local
        env:
          DEFAULT_TAG: ${{ env.BUILD_TAG }}
          DOCKERFILE: ${{ matrix.docker_file }}
      -
        name: Start
        run: |
          docker compose up -d
        working-directory: test/${{ matrix.dir }}
        env:
          NEXTCLOUD_IMAGE: ${{ env.BUILD_TAG }}
          NEXTCLOUD_CONTAINER: ${{ env.CONTAINER_NAME }}
      -
        name: Check container logs
        uses: crazy-max/.github/.github/actions/container-logs-check@d89fe92d808a15e2b2ed5cdb62db7c172c31410d # v1.6.0
        with:
          container_name: ${{ env.CONTAINER_NAME }}
          log_check: "ready to handle connections"
      -
        name: Logs
        if: always()
        run: |
          docker compose logs
        working-directory: test/${{ matrix.dir }}
        env:
          NEXTCLOUD_IMAGE: ${{ env.BUILD_TAG }}
          NEXTCLOUD_CONTAINER: ${{ env.CONTAINER_NAME }}
